% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasso_cv_adjusted.R
\name{lasso_cv_adjusted}
\alias{lasso_cv_adjusted}
\title{Repeated Cross-Validation for LASSO using glmnet}
\usage{
lasso_cv_adjusted(
  x,
  y,
  cv_iter,
  alpha,
  pen = NULL,
  measure = c("mse", "deviance", "class", "auc", "mae"),
  family = c("gaussian", "binomial", "poisson", "multinomial", "cox", "mgaussian")
)
}
\arguments{
\item{x}{Matrix of predictors.}

\item{y}{Response variable.}

\item{cv_iter}{Number of repetitions for cross-validation.}

\item{alpha}{Alpha parameter for glmnet.}

\item{pen}{Optional penalty factor for glmnet.}

\item{measure}{Type of measure to use for cross-validation (default is 'deviance').}

\item{family}{Model family for glmnet.}
}
\value{
A list containing the optimal lambda.min, lambda.1se, and the fitted glmnet model.
}
\description{
This function performs repeated cross-validation for LASSO models using the glmnet package.
It aims to find the optimal lambda with the smallest error. The function returns the lambda
values at minimum error and 1 standard error from the minimum.
}
\examples{
# Assuming x is your predictor matrix and surv is your response variable
cv_results <- glmnet.repcv(x, surv, nreps = 200, alpha = 1, nvar = 50)
}
